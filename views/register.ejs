<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Register</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    label {
        font-size: 18px;
    }
</style>
</head>
<body>
  <!-- Title -->
  <div class="text-center mb-4 mt-5">
    <h1 class="display-2 fw-bold">Todo App</h1>
  </div>

  <div class="container py-5">
    <div class="mx-auto" style="max-width: 100%; width: 100%; max-width: 400px;">
      <!-- Register page header -->
      <h2 class="mb-4 text-center display-5">Register</h2>

      <!-- Error message display -->
      <% if (error) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>

      <!-- Registration form -->
      <form method="POST" action="/register">
        <!-- Username input -->
        <div class="mb-3">
          <label>Username</label>
          <input name="username" class="form-control" required />
        </div>

        <!-- Password input -->
        <div class="mb-3">
          <label>Password</label>
          <input type="password" name="password" class="form-control" required />
        </div>

        <!-- Submit button -->
        <button class="btn btn-primary w-100 fw-bold" type="submit">Register</button>
      </form>

      <!-- Link to login -->
      <p class="mt-3 text-center">Already have an account? <a href="/login">Login here</a></p>
    </div>
  </div>
</body>
</html>
